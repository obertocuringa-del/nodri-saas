'use client'

// A pagina do Check List agora e so a casca: o conteudo vive no painel, que e o
// mesmo usado DENTRO dos setores (cada um com a sua categoria).

import ChecklistPainel from '@/components/salon/ChecklistPainel'

export default function ChecklistPage() {
  return <ChecklistPainel />
}
